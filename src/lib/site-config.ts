/**
 * Global site configuration flags.
 *
 * Pricing is intentionally hidden until the client finalises pricing.
 * Flip `SHOW_PRICES` to `true` and the badges/labels reappear across the site
 * (no other change required).
 */
export const SHOW_PRICES = false
